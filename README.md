# Deno completion plugin for zinit

Install with:

`zinit id-as"deno" as"completion" atclone"source *.zsh" atpull"%atclone" for NotTheDr01ds/zsh-plugin-deno`

When updating deno, if completions have changed, run `zinit update deno`
